[build]
  # Vite's output directory is set to `build` in `vite.config.ts`.
  # Netlify must publish the same folder that the build generates.
  publish = "build"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
